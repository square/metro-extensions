// FILE: comTestFakeMyServiceServiceContributionUnit.kt
package metro.hints

fun Unit(contributed: ServiceContribution) {
  return error(message = "Never called")
}

// FILE: comTestMyServiceServiceContributionUnit.kt
package metro.hints

fun Unit(contributed: ServiceContribution) {
  return error(message = "Never called")
}

// FILE: replacedService.kt
package com.test

@ContributesService(scope = Unit::class, replaces = [MyService::class])
@Inject
class FakeMyService : MyService {
  @ContributesTo(scope = Unit::class, replaces = [MyService::class, ServiceContribution::class])
  @BindingContainer
  @Origin(value = FakeMyService::class)
  object ServiceContribution {
    @Deprecated(message = "This synthesized declaration should not be used directly", level = DeprecationLevel.HIDDEN)
    @CallableMetadata(callableName = "provideMyService", propertyName = "", startOffset = -1, endOffset = -1, newInstanceName = "provideMyService")
    class ProvideMyServiceMetroFactory : Factory<MyService> {
      private /* final field */ val realService: Provider<MyService> = realService
      private /* final field */ val fakeService: Provider<FakeMyService> = fakeService
      private /* final field */ val isFakeMode: Provider<Boolean> = isFakeMode
      companion object Companion {
        private constructor() /* primary */ {
          super/*Any*/()
          /* <init>() */

        }

        @HiddenFromObjC
        @JvmStatic
        @JsStatic
        fun create(@RealService realService: Provider<MyService>, fakeService: Provider<FakeMyService>, @FakeMode isFakeMode: Provider<Boolean>): Factory<MyService> {
          return ProvideMyServiceMetroFactory(realService = realService, fakeService = fakeService, isFakeMode = isFakeMode)
        }

        @HiddenFromObjC
        @JvmStatic
        @JsStatic
        fun provideMyService(@RealService realService: Provider<MyService>, fakeService: Provider<FakeMyService>, @FakeMode isFakeMode: Boolean): MyService {
          return ServiceContribution.provideMyService(realService = realService, fakeService = fakeService, isFakeMode = isFakeMode)
        }

      }

      @HiddenFromObjC
      private constructor(realService: Provider<MyService>, fakeService: Provider<FakeMyService>, isFakeMode: Provider<Boolean>) /* primary */ {
        super/*Any*/()
        /* <init>() */

      }

      @HiddenFromObjC
      override operator fun invoke(): MyService {
        return Companion.provideMyService(realService = <this>.#realService, fakeService = <this>.#fakeService, isFakeMode = <this>.#isFakeMode.invoke())
      }

      @HiddenFromObjC
      fun mirrorFunction(@RealService realService: Provider<MyService>, fakeService: Provider<FakeMyService>, @FakeMode isFakeMode: Boolean): MyService {
        return error(message = "Never called")
      }

    }

    @Deprecated(message = "This synthesized declaration should not be used directly", level = DeprecationLevel.HIDDEN)
    @CallableMetadata(callableName = "provideRealMyService", propertyName = "", startOffset = -1, endOffset = -1, newInstanceName = "provideRealMyService")
    class ProvideRealMyServiceMetroFactory : Factory<MyService> {
      private /* final field */ val serviceCreator: Provider<ServiceCreator> = serviceCreator
      companion object Companion {
        private constructor() /* primary */ {
          super/*Any*/()
          /* <init>() */

        }

        @HiddenFromObjC
        @JvmStatic
        @JsStatic
        fun create(@RetrofitAuthenticated serviceCreator: Provider<ServiceCreator>): Factory<MyService> {
          return ProvideRealMyServiceMetroFactory(serviceCreator = serviceCreator)
        }

        @HiddenFromObjC
        @JvmStatic
        @JsStatic
        fun provideRealMyService(@RetrofitAuthenticated serviceCreator: ServiceCreator): MyService {
          return ServiceContribution.provideRealMyService(serviceCreator = serviceCreator)
        }

      }

      @HiddenFromObjC
      private constructor(serviceCreator: Provider<ServiceCreator>) /* primary */ {
        super/*Any*/()
        /* <init>() */

      }

      @HiddenFromObjC
      override operator fun invoke(): MyService {
        return Companion.provideRealMyService(serviceCreator = <this>.#serviceCreator.invoke())
      }

      @SingleIn(scope = Unit::class)
      @RealService
      @HiddenFromObjC
      fun mirrorFunction(@RetrofitAuthenticated serviceCreator: ServiceCreator): MyService {
        return error(message = "Never called")
      }

    }

    private constructor() /* primary */ {
      super/*Any*/()
      /* <init>() */

    }

    @Provides
    fun provideMyService(@RealService realService: Provider<MyService>, fakeService: Provider<FakeMyService>, @FakeMode isFakeMode: Boolean): MyService {
      return when {
        isFakeMode -> fakeService.invoke()
        else -> realService.invoke()
      }
    }

    @Provides
    @SingleIn(scope = Unit::class)
    @RealService
    fun provideRealMyService(@RetrofitAuthenticated serviceCreator: ServiceCreator): MyService {
      return serviceCreator.create<MyService>(service = <get-java></* null */>(/* <this> = MyService::class */))
    }

  }

  @Deprecated(message = "This synthesized declaration should not be used directly", level = DeprecationLevel.HIDDEN)
  object MetroFactory : Factory<FakeMyService> {
    private constructor() /* primary */ {
      super/*Any*/()
      /* <init>() */

    }

    @HiddenFromObjC
    @JvmStatic
    @JsStatic
    fun create(): Factory<FakeMyService> {
      return MetroFactory
    }

    @HiddenFromObjC
    @JvmStatic
    @JsStatic
    fun newInstance(): FakeMyService {
      return FakeMyService()
    }

    @HiddenFromObjC
    override operator fun invoke(): FakeMyService {
      return MetroFactory.newInstance()
    }

    @HiddenFromObjC
    fun mirrorFunction(): FakeMyService {
      return error(message = "Never called")
    }

  }

  constructor() /* primary */ {
    super/*Any*/()
    /* <init>() */

  }

}

@ContributesService(scope = Unit::class)
@RetrofitAuthenticated
interface MyService {
  @ContributesTo(scope = Unit::class)
  @BindingContainer
  @Origin(value = MyService::class)
  object ServiceContribution {
    @Deprecated(message = "This synthesized declaration should not be used directly", level = DeprecationLevel.HIDDEN)
    @CallableMetadata(callableName = "provideMyService", propertyName = "", startOffset = -1, endOffset = -1, newInstanceName = "provideMyService")
    class ProvideMyServiceMetroFactory : Factory<MyService> {
      private /* final field */ val serviceCreator: Provider<ServiceCreator> = serviceCreator
      private /* final field */ val isFakeMode: Provider<Boolean> = isFakeMode
      companion object Companion {
        private constructor() /* primary */ {
          super/*Any*/()
          /* <init>() */

        }

        @HiddenFromObjC
        @JvmStatic
        @JsStatic
        fun create(@RetrofitAuthenticated serviceCreator: Provider<ServiceCreator>, @FakeMode isFakeMode: Provider<Boolean>): Factory<MyService> {
          return ProvideMyServiceMetroFactory(serviceCreator = serviceCreator, isFakeMode = isFakeMode)
        }

        @HiddenFromObjC
        @JvmStatic
        @JsStatic
        fun provideMyService(@RetrofitAuthenticated serviceCreator: ServiceCreator, @FakeMode isFakeMode: Boolean): MyService {
          return ServiceContribution.provideMyService(serviceCreator = serviceCreator, isFakeMode = isFakeMode)
        }

      }

      @HiddenFromObjC
      private constructor(serviceCreator: Provider<ServiceCreator>, isFakeMode: Provider<Boolean>) /* primary */ {
        super/*Any*/()
        /* <init>() */

      }

      @HiddenFromObjC
      override operator fun invoke(): MyService {
        return Companion.provideMyService(serviceCreator = <this>.#serviceCreator.invoke(), isFakeMode = <this>.#isFakeMode.invoke())
      }

      @SingleIn(scope = Unit::class)
      @HiddenFromObjC
      fun mirrorFunction(@RetrofitAuthenticated serviceCreator: ServiceCreator, @FakeMode isFakeMode: Boolean): MyService {
        return error(message = "Never called")
      }

    }

    private constructor() /* primary */ {
      super/*Any*/()
      /* <init>() */

    }

    @Provides
    @SingleIn(scope = Unit::class)
    fun provideMyService(@RetrofitAuthenticated serviceCreator: ServiceCreator, @FakeMode isFakeMode: Boolean): MyService {
      when {
        isFakeMode -> error(message = "No fake service provided for MyService.")
      }
      return serviceCreator.create<MyService>(service = <get-java></* null */>(/* <this> = MyService::class */))
    }

  }

}
